import { googleapis } from './apis';

export type GoogleApis = typeof googleapis;
export type FirebaseApi = typeof googleapis.firebase;
export type CloudResourceManagerApi = typeof googleapis.cloudresourcemanager;
